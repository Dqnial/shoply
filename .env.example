# Used by docker-compose.yml (Docker Compose auto-loads a ".env" file next
# to it). Only needed for `docker compose up` — copy to ".env" and set a
# real secret before running anything beyond local experimentation.
JWT_SECRET=change-me-to-a-long-random-string
