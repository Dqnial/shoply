# Not needed for normal use — the unified server (backend/src/server.ts)
# serves the frontend and API from the same origin, so relative "/api" and
# "/uploads/..." paths already resolve correctly without this. Only set it
# if the API genuinely lives on a different origin than the frontend.
# NEXT_PUBLIC_API_URL=http://localhost:5000
